<%= form_with(model: notary) do |form| %>
  <% if notary.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(notary.errors.count, "error") %> prohibited this notary from being saved:</h2>

      <ul>
        <% notary.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
